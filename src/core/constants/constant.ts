export enum SubCategoryType {
  EVENT = 'EVENT',
  GROUP = 'GROUP',
}

export enum Status {
  Draft = 'draft',
  Pending = 'pending',
  Published = 'published',
}

export enum GroupRole {
  Owner = 'owner',
  Moderator = 'moderator',
  Member = 'member',
}
